# 手绘量房识别项目交接报告

> 历史记录：本文仅保留当时的排查背景，不代表当前功能状态或推荐配置。当前安装、配置和测试方式以 `README.md` 为准。

日期：2026-07-28

## 1. 当前结论

项目尚未达到可交付状态。轮廓识别已能在当前样例上得到正确的 12 个正交折点，但尺寸读取、尺寸归属、墙段填入和门洞生成仍未形成可信的单一数据链。

最重要的问题不是单个提示词，而是目前存在三套相互不完全同步的数据：

1. `observations`：右侧“图片校正”显示的 OCR/视觉证据。
2. `plan_annotation.edge_chain`：标注页上方 `W1-W12` 的墙段长度，建模前闭合计算读取这里。
3. `openings`：二维/三维真正使用的门窗对象。

右侧确认一个值并不保证上方墙长和门洞同时更新；分析阶段也可能分别写入三处。用户因此无法判断哪份数据是真正用于建模的数据。

## 2. 当前运行数据不可用

当前项目：

- 项目 ID：`aa0d259bc750466d923438c1a43604a6`
- 图片资产 ID：`3874a751cbab49268e53eb2a175d0bb5`
- 图片：`test0.jpg`
- 当前轮廓：12 点
- 当前 observations：109 条
- 当前 openings：0 条

当前数据库中的错误边长为：

```text
W1=9640
W2=400
W3=320
W4=2855
W5=1810
W6=12980
W7=615
W8=null
W9=610
W10=9025
W11=2540
W12=615
```

这些值来自一个失败的本地回退策略：它把同一墙附近的重复识别、总尺寸和误识别全部相加。不要继续使用或保存为真实测量数据。完成修复后应重新分析项目，或创建新项目重新上传样例。

## 3. 当前样例的人工核对基准

当前 12 点方向顺序：

```text
W1  down
W2  right
W3  up
W4  right
W5  up
W6  left
W7  down
W8  left
W9  up
W10 left
W11 down
W12 left
```

从样例图人工读取的可编辑初值：

```text
W1  = 1840
W2  = 1255  (400 + D1 800 + 55)
W3  = 320
W4  = 2855
W5  = 1810  (图上该处读数；用户可再修改)
W6  = 1590
W7  = 610
W8  = 615
W9  = 610
W10 = 1640
W11 = 320
W12 = 260
```

闭合误差：

```text
水平：1255 + 2855 = 4110
水平反向：1590 + 615 + 1640 + 260 = 4105
误差：5 mm

竖向：1840 + 610 + 320 = 2770
竖向反向：320 + 1810 + 610 = 2740
误差：30 mm
```

现有 1.5% 有界闭合容差可以吸收这两处实测误差，并保留原始读数。

门洞基准：

```text
编号：D1
CG：0
CK：800
CH：2055
所在墙：W2（代码 wall_index=1）
距 W2 起点：400
墙后剩余：55
洞口厚度：应保留可编辑字段，当前可用默认 100 mm
```

## 4. 用户已经明确的业务规则

- 只使用一种手绘颜色，不要求三种颜色。
- 净高写中间；吊顶高度对应区域，不把高度与点位混在一起。
- 未标注基准面时默认完成面。
- 门窗统一使用 `CG / CK / CH`：洞口距地、洞口内宽、洞口内高。
- 门窗需要厚度。
- 点位不通过文字定位尺寸表录入；识别图中点位符号和相对位置，生成后可拖动。
- 草图线段像素长度与实际毫米长度无关，禁止按像素比例换算。
- 实测尺寸允许小误差；建模前通过正交闭合方程吸收容差。
- 轮廓只有 0 度和 90 度墙段；零长、斜边、自交必须禁止。
- 识别不准可以让用户简单修改，但不能所有墙长均为空。
- 增删折点后，未受影响墙段的原数值必须保留，不能随 `W` 编号错位。
- 图片识别只使用 `READ_MODEL`；`CHAT_MODEL` 作为非视觉计算协调器。

## 5. 已验证有效的部分

### 5.1 轮廓与建模前校验

- 当前样例已实际识别出 12 个折点。
- 门洞不作为假折点；共线墙面跨过门洞。
- 建模前拒绝零长边、斜边和自交。
- 实测误差闭合逻辑已存在，且有前后端测试。

### 5.2 增删折点后保持未受影响边长

已在 `src/geometry.ts` 新增 `reconcileBoundaryEdges()`，按“有向起点和终点”匹配旧、新墙段，不再按数组下标继承。

已覆盖并通过 6 个前端几何测试，其中包括：

- 插入折点：被拆分边清空，其他边保留。
- 删除折点：被合并边清空，其他边保留。
- 移动折点：只清空相邻两边。

`src/components/PhotoAnnotation.tsx` 已改为使用该函数。

### 5.3 兼容接口输出长度

- 请求层会按兼容接口能力限制 `max_tokens`。
- 边长输出已改为紧凑数组协议：`lengths_mm` 和 `evidence_ids`。
- 完整 12 项骨架可以避免模型少输出数组位置。

## 6. 失败原因与已确认事实

### 6.1 逐墙裁片识别会放大网格误识别

原实现不是 OCR 自己决定裁切位置，而是程序按识别出的墙段生成裁片，再让视觉/OCR读取裁片。

当前模板带规则网格。窄墙段裁片中，网格线和尺寸线占比过高，模型会把网格形状误读成数字，并把相邻墙、总尺寸或重复读数绑定到当前墙。

缓存中曾有 88 条 `wall-crop-vision` token，已经覆盖/污染原整图 OCR token。错误值 `9640/12980/9025` 即由这些候选累加产生。

用户观察正确：整图 OCR 的多个尺寸读得较好，逐墙裁片反而更差；整图 OCR 仍有少数离谱读数，需要视觉复核，不能直接使用。

### 6.2 “裁切读取”和“实际填入”必须拆开

正确流程应是：

```text
整图 OCR 候选
  + 均匀重叠视觉分块补漏/复核
  -> 恢复全图 bbox
  -> 重叠去重、网格假数字过滤
  -> 干净的只读 Evidence 集合
  -> 独立尺寸归属/计算
  -> edge_chain + openings
  -> 二维/三维建模
```

禁止使用以下逻辑：

```text
某个裁片围绕 wall:N -> 裁片中的数字自动属于 wall:N
```

### 6.3 门洞未生成的直接原因

后端 `_opening_specs_from_tokens()` 只有在门宽证据同时存在于完整墙段尺寸链中时才生成 opening。边链为空或错误时，D1 即使识别为 `CG 0 CK 800 CH 2055`，也不会生成门洞。

前端还有一个明确错误：标注阶段 `RoomSpec.boundary` 尚未生成，`applyEvidence()` 却用 `next.boundary.length` 校验门墙，因此右侧确认 D1 时会被跳过。

本轮已局部修改为使用 `plan_annotation.boundary.length`，但尚未完成端到端测试。

## 7. 本轮最后几次修改的状态

以下代码已写入工作区，但没有完成端到端验收，不应直接视为完成：

### 7.1 后端均匀分块和去网格尝试

`backend/app/ai.py` 中新增/修改：

- `WALL_CROP_CACHE_VERSION = 12`，用于废弃旧污染缓存。
- 快速分析主路径已停止调用 `_recognize_wall_crops_with_vision()`。
- 新增 6 个均匀重叠区域 `TEMPLATE_TILE_VIEWS`。
- `_collect_template_evidence()` 改为并发读取 6 个区域并恢复全图 bbox。
- 新增 `_handwriting_mask()`、`_handwriting_ink_image()`、`_evidence_has_handwriting()`。
- 每个视觉块同时提供原图和笔迹隔离图，要求只接受隔离图中真实存在的手写内容。

风险：

- 笔迹阈值 `HSV saturation >= 35 / gray <= 105` 尚未对多种笔色、黑笔和不同曝光做校准。
- 新的 6 块视觉读取尚未对当前样例完成真实联网分析。
- 现有单元测试没有充分验证分块拼接、bbox 映射和网格过滤。

### 7.2 非视觉协调器尝试

新增 `_coordinate_segment_edge_chain()`，计划让 `CHAT_MODEL` 只读取：

- 12 段拓扑与方向；
- 已拼接的全图坐标证据；
- 尺寸链关系；
- 完整 12 项输出骨架。

第一次真实调用超过 180 秒并超时，没有结果。之后已关闭该接口的思考模式，但没有再次验证。

### 7.3 前端同步尝试

`src/App.tsx` 已局部增加：

- 右侧确认 `wall_segment` 时更新对应 `plan_annotation.edge_chain[wallIndex]`。
- 确认 `door_size` 时用 `plan_annotation.boundary` 校验墙号，并优先用 `edge_chain` 长度计算 offset。

尚未完成：

- 这部分修改之后没有重新运行 `npm run build` 和前端测试。
- 上方手工修改 `W` 仍不会同步回对应 observation。
- 多段尺寸 `400 + 800 + 55` 如何在右侧分别确认后合并为 W2，尚未形成唯一规则。
- 自动分析完成后如何统一重建 openings，尚未实现纯函数。

## 8. 测试状态

最后一次可靠记录：

- 前端几何聚焦测试：6 项通过。
- 后端边链/模板聚焦测试：9 项通过。
- 较早版本后端相关测试：101 项通过。
- 较早版本全量记录：后端 146 项、前端 14 项、契约 50 项通过。
- `npm run build` 在最后一次 `src/App.tsx` 同步修改之前通过。

因此当前工作区必须重新执行完整测试，不能引用较早通过数作为当前结论。

## 9. 交接时的服务状态

- 交接时运行的后端进程和 `dist` 均早于最后几轮修改，不能作为当前验证依据。
- 后续验证必须重新构建并启动服务，以 README 中的测试命令和实际结果为准。

## 10. 建议的接手顺序

### 第一步：先建立唯一数据源，不继续调提示词

建议定义一个纯函数或服务，例如：

```text
rebuildDerivedMeasurement(evidence, plan_annotation)
  -> edge_chain
  -> openings
  -> height
  -> fixtures
```

规则：

- `observations/evidence` 保存原始读数、bbox、置信度、人工修正和归属。
- `edge_chain/openings` 是唯一建模输入，由上述函数生成并允许用户覆盖。
- 每个结构值必须保存 `evidence_ids`；UI 显示来源。
- 用户在右侧确认、上方修改墙长或修改门洞时，都调用同一重建/覆盖逻辑。
- 删除证据时必须同步删除或降级引用它的结构值。

### 第二步：彻底清除逐墙裁片污染

- 保持快速主路径不调用 `_recognize_wall_crops_with_vision()`。
- 不要仅仅隐藏 UI 框；必须保证污染 token 不进入 `observations`、绑定器和边长求解器。
- 缓存升级后用全新项目或删除对应 OCR 缓存重新分析。

### 第三步：验证“整图 OCR + 均匀视觉块”读取层

对当前样例先只输出一张证据表，不填墙：

```text
id | text | global bbox | source | repeated count | accepted/rejected
```

验收要求：

- 读到 1840、400、800、55、320、2855、1810、1590、610、615、1640、260。
- 读到 D1 CG 0 CK 800 CH 2055。
- 网格线不生成数字证据。
- 4105/4110 可作为总尺寸校验，但不能直接塞给局部墙。
- 离谱孤立值不进入 accepted 集合。

只有读取层通过后再做归属。

### 第四步：实现确定性的归属与闭合

- 模型只提出 `evidence_id -> wall_index` 候选。
- 程序校验方向、尺寸链顺序、证据是否重复、闭合方程。
- W2 允许连续链 `400 + 800 + 55 = 1255`。
- 5 mm 和 30 mm 的实测误差保留原始值，由现有闭合容差吸收。
- 禁止按草图像素长短选择毫米数。

### 第五步：门洞必须从同一证据派生

当 D1 证据被确认并绑定 W2 后，应一次生成：

```text
opening.kind = door
opening.wall_index = 1
opening.offset_mm = 400
opening.width_mm = 800
opening.height_mm = 2055
opening.sill_mm = 0
opening.thickness_mm = 100 (可编辑)
opening.evidence_ids = [D1 行证据, 400, 800, 55]
```

照片标注、二维图和三维模型必须读取同一个 `opening` 对象。

### 第六步：端到端浏览器验收

必须实际操作浏览器验证：

1. 新建项目并上传 `test0.jpg`。
2. 分析后只显示有效证据框，不显示网格假框。
3. W1-W12 有合理初值。
4. D1 在 W2 上显示为 `400 + D1 800 + 55`。
5. 修改右侧证据，上方 W 和门洞立即同步。
6. 修改上方 W 后，建模读取修改值。
7. 插入/删除/拖动折点，未受影响边长不串位。
8. 闭合后生成二维和三维，门洞真实切开墙体并保留厚度。

## 11. 相关文件

- 后端识别主逻辑：`backend/app/ai.py`
- 后端模型：`backend/app/models.py`
- 后端 API：`backend/app/main.py`
- 前端主状态与证据应用：`src/App.tsx`
- 照片标注与墙长输入：`src/components/PhotoAnnotation.tsx`
- 右侧证据校正：`src/components/EvidenceReview.tsx`
- 几何闭合和边匹配：`src/geometry.ts`
- 几何测试：`src/geometry.test.ts`
- 当前样例：`test0.jpg`
- 当前数据库：`backend/data/studio.sqlite3`
- OCR 缓存：`.tmp/ocr-fast/`
- 模型 trace：`backend/data/ai-traces/`

## 12. 工作区注意事项

工作区包含大量此前已有的未提交修改和新增文件。不要执行 `git reset --hard`、`git checkout --` 或批量回退。接手者应先阅读 `git status` 和相关 diff，只处理上述识别链路。

本轮创建了诊断脚本 `.tmp/run_edge_resolver.py`，仅用于单独调用边长求解；它不是正式产品代码，可在确认无用后删除。
