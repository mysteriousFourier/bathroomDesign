# Release Notes

## v1.0.1 - 2026-08-31

本次更新聚焦自动布局约束、语音降级体验和启动依赖选项。

### 更新内容

- 明确湿区、干区和设备服务区约束，空间不足时按设备优先级生成可执行布局。
- 增强布局方案的几何校验与前端方案切换稳定性。
- 服务端语音合成不可用时，设计对话和问候语可回退到浏览器语音播放。
- 录音格式检测和静音判断兼容更多浏览器与嘈杂环境。
- Windows 启动器支持 `-WithoutVoice` 轻量安装选项。

### 已知限制

- 本版本仍用于功能验证，不内置身份认证、权限管理、HTTPS 终止或租户隔离。

## v1.0.0 - 2026-08-31

首个可分发的功能测试版本。

### 主要能力

- 从手绘量房图、现场照片及 JSON/GeoJSON/SVG/DXF 文件生成可追溯的结构化量房数据。
- 提供二维审图、尺寸及洞口编辑、固定设施校核和测量证据回看。
- 从确认后的同版量房数据生成三维空间，并支持多级自动布局、产品选型和 GLB 导出。
- 内置卫生间产品与墙地面材质库；完整发布包包含对应本地模型资源。
- 提供设计需求对话；可选安装本地语音转写和语音合成依赖。
- 支持 Windows 一键启动和 Docker Compose 部署。

### 本版改进

- 提升二维视图、自动点位设置和平面图识别的稳定性。
- 完善自动布局约束、方案分级和产品绑定流程。
- 增加量房导入草稿保护，失败或未确认的解析不会覆盖上一版数据。
- 发布包增加预构建前端、完整模型资源、SHA-256 校验和部署测试指南。
- Windows 启动器可自动安装项目内便携工具，并将语音依赖改为按需安装。

### 已知限制

- 本版本用于受控测试，不内置身份认证、权限管理、HTTPS 终止或租户隔离。
- 视觉识别依赖部署方提供的兼容多模态模型，准确率受图纸规范、拍摄质量和模型能力影响。
- 语音转写首次启用需要下载本地模型，TTS 默认还需要访问外部服务。
- DWG 导入依赖外部 ODA File Converter 或 LibreDWG；未安装时需先转换为 DXF。
- 自动生成结果必须由具备专业能力的人员复核，不能直接用于施工。
