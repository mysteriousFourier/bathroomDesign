# 自动布局任务要求

## 目标

让同一量房房型稳定生成三套可应用方案：经济档、舒适档、品质档。三档都必须正常，不能把某一档标为硬错误后仍展示给用户。

## 必须按此顺序实现

1. **先做分档选品**
   - 先从产品知识图谱和真实模型资产中确定三套家具组合，再进入空间布局。
   - 每档使用真实 `product_id`、目录编号和已绑定模型。
   - 价格按经济、舒适、品质递增；家具组合和数量要有明确的档位差异，不能只改标题或随机换模型。
   - 必需品类不能缺失；禁止推荐规则明确禁止的设备。

2. **再做模型绑定**
   - 禁止使用早期占位模型、proxy 模型或错误品类模型。
   - 模型原始宽、深、高必须保留，禁止为了塞进房间任意拉伸或压缩。
   - 马桶必须绑定正确的真实模型，并服从实测排水点；其它设备也必须绑定到对应位置。

3. **最后做几何布局**
   - 对三套已确定的家具组合分别求解坐标、旋转、墙面和水电点。
   - 保留真实尺寸、墙板完成面间隙、门区、使用净空和碰撞约束。
   - 任一档出现硬错误时，修正该档或整体失败，不能把坏档位混入三档结果。

## 前端验收

- 生成逐墙饰面后，墙面数值、材料数量和三维显示必须同步变化。
- 生成后必须显示三档，并且可以逐档切换和应用到三维预览。
- 三档的产品、家具数量、总价、坐标和布局不能只复制同一套结果。
- 方案切换不能要求用户手动重新绑定模型。

## 数据与日志

- AI trace 只能保留有限期限和数量，禁止无限增长。
- 当前默认上限：保留 30 天、最多 2000 个 JSON、总大小最多 50 MB。
- 清理只能作用于 `backend/data/ai-traces/*.json`，不能删除项目、量房证据、模型资产或 OCR 缓存。

## 验收命令

```text
npm test -- --run
npm run build
.venv\\Scripts\\python.exe -m pytest backend/tests -q
```

验收时重点检查：三档均无 `severity=error`、模型资产和尺寸正确、马桶位置服从排水点、逐墙饰面数值变化、方案可切换，以及 trace 目录不会超过上述限制。
